# RHS HOME 80 Fast Track

Deploy on Vercel:

1. Upload this folder to a new GitHub repo, or unzip locally.
2. In Vercel, create a new project and import the repo.
3. Framework preset: Vite
4. Build command: npm run build
5. Output directory: dist

Local development:

npm install
npm run dev
